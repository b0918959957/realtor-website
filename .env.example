# 這個教學專案預設不需要任何金鑰。
# 預約資料只會寫到 data/appointments.json。
# 確認信只會產生本機 HTML 預覽，不會真的寄出。

# ── Slack 通知（選用）────────────────────────────────
# 設定後，每當有客戶送出預約，就會推播到指定的 Slack 頻道。
# 留空或不設定 = 不啟用，預約流程完全不受影響。
#
# 取得方式：https://api.slack.com/apps → Create New App
#   → Incoming Webhooks → 開啟 → Add New Webhook to Workspace
#   → 選擇要接收通知的頻道 → 複製產生的網址
#
# 使用方式：複製本檔案為 .env.local，填入網址後重啟伺服器。
# 注意：.env.local 已被 .gitignore 排除，不會被提交到 Git。
SLACK_WEBHOOK_URL=
