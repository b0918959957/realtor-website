# 複製本檔案為 .env.local 後填入。.env.local 已被 .gitignore 排除，不會提交到 Git。

# ── LINE 通知（主要通知管道）────────────────────────
# 客戶送出聯絡表單後，會把客戶的完整資料推播到你的 LINE。
#
# 取得步驟：
# 1. 到 https://developers.line.biz/console/ 用你的 LINE 帳號登入
# 2. 建立 Provider（提供者），名稱隨意
# 3. 建立 Messaging API channel，並連結你的官方帳號 @wyb2506c
# 4. 在「Messaging API」分頁最下方，發行 Channel access token（long-lived）
#    → 填到 LINE_CHANNEL_ACCESS_TOKEN
# 5. 在「Basic settings」分頁找到 Your user ID（U 開頭那串）
#    → 填到 LINE_TO_USER_ID
# 6. 用手機 LINE 加自己的官方帳號 @wyb2506c 為好友（否則收不到推播）
LINE_CHANNEL_ACCESS_TOKEN=
LINE_TO_USER_ID=

# ── Slack 通知（備援管道，選用）──────────────────────
# 設定後會同時推一份到 Slack，LINE 若失效還有備援。
# 取得方式：https://api.slack.com/apps → Create New App
#   → Incoming Webhooks → 開啟 → Add New Webhook to Workspace
SLACK_WEBHOOK_URL=

# ── 正式網址（選用）─────────────────────────────────
# 用於 SEO 與社群分享圖片。部署到 Vercel 會自動帶入，通常不需設定。
NEXT_PUBLIC_SITE_URL=
